version: "3"
services:
  spovizz_app:
    container_name: ${INSTANCEID}
    restart: always
    image: h2908577.stratoserver.net:5001/spovizz/${INSTANCEID}:6.1.9
    env_file:
      - .env.prod.womansworldcup
networks:
  default:
    external:
      name: live_network
